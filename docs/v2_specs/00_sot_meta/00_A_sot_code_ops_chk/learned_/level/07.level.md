[최종 검토일: 2026-01-26]
정책 최신화 필요 여부: 🟢 최신 (2026-01-26 기준)

## 정책 정합성/충돌 처리 원칙
- SoT-코드-운영-DB-프론트 매핑에서 부정합/충돌 발견 시 아래와 같이 명시:
  - 🔴 [정책/구현 충돌]: 구체적 내용, 우선 기준, 임시 조치/추후 액션 명시
  - 🟡 [정합성 검토 필요]: 불일치/불명확/운영상 임시 허용 등
- 근거/판단 기준(SoT/코드/운영/DB/프론트 우선 등) 및 작성일/정책 문서/운영 사례 등 명확히 기입
- 즉시 수정 불가 시 "임시 예외", "운영상 임시 허용", "추후 일괄 정비 필요" 등 TODO로 남김
- 자동화/운영 체크리스트에 해당 부정합이 감지되도록 추가
- 실제 장애/오류/운영 리스크 시 "운영자 주의", "긴급 패치 필요", "QA 우선 검증" 등 강조

## 자동화/운영 체크리스트 & TODO
- [ ] SoT-코드-운영-DB-프론트 매핑 1:1 정합성 검증 자동화 필요
- [ ] **DB 스키마 검증**: `user_level_progress.xp` 컬럼 존재 및 `v2_user` 내 XP 컬럼 부재 확인
- [ ] **Legacy API 검증**: `/api/v2/roulette/status` 등에서 `season_pass` 필드가 `null`인지 자동 체크
- [ ] **레벨 보상 정합성**: `v2_level_reward_table` 데이터가 SoT(Lv 1~20)와 일치하는지 주기적 비교
- [ ] **CC 적립 모니터링**: `user_xp_event_log`에서 `source=CC_DEPOSIT`인 로그의 `delta`가 일일 최대 100 XP(5스텝) 이내인지 검증 (2026-01-27 조정됨)
- [x] **일일 한도 조정**: 일일 최대 XP 획득량을 1000 XP -> 100 XP(5스텝)로 하향 조정 완료 (관리자 요청)

- [ ] 정책/구현/운영 불일치 발견 시 즉시 표기 및 TODO/임시 예외 명시
- [ ] 최신 정책/운영 사례 반영 주기적 검토(작성일/최종 검토일 갱신)
#
...existing code...
# Level(레벨/레벨포인트) 영역 SoT-코드-운영-DB-프론트 매핑 표 (관리자 친화형)

## 1. 레벨/레벨포인트 정책/핵심 Enum/상수/제약조건

### [A] 주요 DB 컬럼/제약조건/Enum
| DB 테이블/컬럼                | 제약조건/Enum/설명                                   | 정책/코드/프론트 매핑 필드명         | 비고 |
|-------------------------------|------------------------------------------------------|--------------------------------------|------|
| user_level_progress.xp        | INT, NOT NULL                                        | level_point, GAME_XP                 | 레벨포인트 보유량 (오직 GAME_XP로만 적립) |
| user_xp_event_log.delta       | INT, NOT NULL                                        | level_point_delta                    | 레벨포인트 변동 로그 (+/-) |
| user_level_reward_log.reward_type | ENUM(RewardType)                                 | level_point_reward_type              | 레벨 보상 타입 |
| **-> ENUM(RewardType)**       | `POINT`, `CC_POINT`, `GAME_XP`, `DIAMOND`, `TICKET`, `BUNDLE`, `TICKET_BUNDLE`, `NONE` | (표준 RewardType) | **SoT**: `v2_reward_type_standard_sot_ko.md` |
| **-> Ticket (Payload)**       | `ROULETTE`, `DICE`, `LOTTERY`, `GOLD_KEY`, `DIAMOND` | (티켓 명칭) | **SoT**: `v2_ticket_enum_sot_ko.md` |
| user_level_reward_log.reward_payload | JSON                                            | payload                              | (예: `{"amount": 500}`) |
| v2_user.cc_id                 | VARCHAR, UNIQUE                                      | cc_id                                | 외부 식별자 (필수) |
| v2_user.nickname              | VARCHAR, INDEX                                       | nickname                             | 검색용 (Nullable) |


### [B] 프론트-백엔드-DB-코드-정책 1:1 매핑 구조
| 정책/문서           | 실제 코드/Enum/상수         | DB 컬럼/제약조건                | 프론트/API 필드명         | 비고 |
|---------------------|-----------------------------|----------------------------------|----------------------|------|
| v2_level_point_sot_ko.md | level_point, GAME_XP | user_level_progress.xp | `user.level` (Int), `user.level_point` | **Auth API**: `/api/auth/token` |
| v2_level_point_storage_sot_ko.md | level_point_delta | user_xp_event_log.delta | `delta` | 변동 로그 |
| v2_level_reward_table_sot_ko.md | level_point_reward_type, RewardType | user_level_reward_log.reward_type | `reward_type`, `xp_reward` | **Mission API**: `xp_reward` (Default 0) |
| v2_progression_schema_ko.md | (Season Pass 폐기) | (사용 안함) | `season_pass: null` | **Legacy API**: 항상 `null` 반환 필수 |
| v2_level_point_extension_sot_ko.md | `CC_DEPOSIT` (Role) | `user_xp_event_log.source` | - | 10만 원당 20XP 적립, **일일 최대 100XP 제한** (BE 로직) |


---

## 2. 레벨/레벨포인트 정책/DB/정합성/상수/Enum (운영 매핑 표)
| 구분 | SoT 문서/정책/스키마 | SoT 한글 설명/핵심값/상수/필드 | 실제 코드/핵심 파일 | 운영 상태/테스트/DB/엔드포인트 | DB 적용값 | V1 폐기 | V2 이관 | FE 라우팅 | FE 표시값 | 최신화 일자 | 검증 결과 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 레벨포인트 정책 | v2_level_point_sot_ko.md | "레벨포인트=GAME_XP, 금고포인트와 무관, 오직 GAME_XP로만 적립" | app/v2/models/level.py | /api/v2/level/*, test_level_*.py | | | ✅ 이관 | /level | 레벨 | 2026-01-26 | ✅ 완료 | level_reward_table_20260126.md 기준 |
| 레벨포인트 저장 | v2_level_point_storage_sot_ko.md | "user_level_progress.xp, user_xp_event_log.delta, user_level_reward_log.reward_type" | app/v2/models/level.py | DB: user_level_progress, user_xp_event_log, user_level_reward_log | | | ✅ 이관 | | | 2026-01-26 | ✅ 완료 | |
| 레벨 보상표 | v2_level_reward_table_sot_ko.md | "레벨 1~20, 필요 XP/보상, 티켓 용어 일관성, CC 입금 적립 규칙" | app/v2/models/level.py | DB: user_level_progress, user_level_reward_log | | | ✅ 이관 | | | 2026-01-26 | ✅ 완료 | Phase 5 E2E 로그 확인 |
| 레벨포인트 확장 | v2_level_point_extension_sot_ko.md | "CC 입금 10만당 20포인트 적립, 일일 최대 100 XP 제한" | app/v2/models/level.py | 정책: CC/게임별 확장 | | | ✅ 이관 | | | 2026-01-27 | ✅ 완료 | CC Deposit 일일 한도 100XP 조정 적용 |
| Season Pass | v2_progression_schema_ko.md | "V2에서 폐기됨 (단일 레벨 시스템으로 통합)" | (제거됨) | - | - | ✅ 폐기 | - | - | - | 2026-01-26 | ✅ 완료 | Legacy API null 반환 확인 |

<!-- 각 표에 FK/UNIQUE/ENUM 제약조건, 프론트-백엔드-DB-코드-정책 1:1 매핑 구조가 명확히 드러나도록 작성, 최신화/검증 결과/비고는 수동 또는 자동화 스크립트로 채움 -->

## 3. 정합성 검증 요약 리포트 (Step 2-2)

- **상태**: 🟢 **정합 (Validated)**
- **주요 발견 사항**:
    - 🟢 [DB 분리]: `v2_user` 테이블에 레벨/XP 컬럼이 없음을 확인. 성장 데이터는 `user_level_progress` 테이블로 물리적/논리적 분리 완료 (User Identity vs Progression 분리 원칙 준수).
    - 🟢 [정책 준수]: 'Season Pass' 용어 폐기 정책 확인. Legacy API (`/status` 등)에서 `season_pass` 필드가 존재하나 `null`을 반환하도록 규약 준수됨.
    - 🟢 [단일 레벨]: Core/Game/Mission 전 영역에서 `level_point` (GAME_XP) 단일 재화 사용 및 적립 로직 일치 확인.
- **조치 사항**:
    - 🟡 [모니터링]: `CC_DEPOSIT` 시 10만 원당 20XP 적립 로직이 `user_xp_event_log`에 정상 적재되는지 운영 초기 집중 모니터링 권고.

## 4. 실전 코드 검증 리포트 (Step 3-3)

- **검증 대상**: `app/models/level_xp.py`, `app/v2/services/admin_cc_deposit_service.py`
- **검증 일시**: 2026-01-26
- **주요 발견 사항**:
    - 🟢 [DB 구현 정합]: `UserLevelProgress` 및 `UserLevelRewardLog` 모델이 문서와 일치함. 단, `reward_type`은 코드상 `String(50)`으로 유연하게 구현됨 (Policy Enum 준수 권고).
    - 🟡 [Legacy 동기화]: `admin_cc_deposit_service.py`에서 `level_xp.add_xp`와 `season_pass.add_bonus_xp`가 동시에 호출됨(Dual Write). 과도기적 동기화 로직으로 판단되나, 추후 제거 필요.
    - 🟢 [CC 적립 로직]: `STEP_AMOUNT = 100_000`, `XP_PER_STEP = 20` 상수가 코드에 정확히 구현됨.
- **최종 결론**: **✅ 운영 배포 가능 (Ready to Ops)**. 단, Season Pass 레거시 코드는 차기 배포 시 제거 스케줄링 필요.


