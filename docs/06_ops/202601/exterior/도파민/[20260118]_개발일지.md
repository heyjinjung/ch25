# 🛠️ 개발일지 (2026-01-18)

> **작성자:** Antigravity (AI Assistant)
> **작성시각:** 2026-01-18 10:43 (KST)
> **주요 작업:** 운영일지 고도화 (데이터 정합성 및 방문자 추적)

---

## 1. 🐞 버그 수정 (Fixes)

### A. 다이아몬드 수량 표시 오류 수정 
*   **증상**: 운영일지에 다이아몬드 보유량이 `7개`로 과소 집계됨 (실제 63개).
*   **원인**: `user_game_wallet` 테이블을 SoT로 사용했으나, 실제 다이아몬드는 `user_inventory_item` (Item Type: `DIAMOND`) 테이블에 저장됨. (경제체계 SoT 확인 결과)
*   **해결**: `daily_ops_log_generator.py`의 `get_inventory_snapshot` 함수 내 다이아몬드 조회 쿼리를 `user_inventory_item` 대상으로 변경.
    ```python
    # After
    diamond_query = "SELECT SUM(quantity) FROM user_inventory_item WHERE item_type = 'DIAMOND'"
    ```
*   **결과**: 운영일지에 `63개`로 정상 표기됨.

### B. 금고 구매(복권 등) 내역 누락 이슈 해결 (Critical)
*   **증상**: 유저가 금고 예치금(Vault)으로 복권 등을 구매했으나, 거래 내역(Ledger)이 남지 않고 출금 조건(일일 사용량)에도 반영되지 않음.
*   **원인**: SSH 접속 등을 통한 비정상적/수동 구매 시도(스크립트 추정)로 인해 `VaultService.consume_locked_balance`의 표준 트랜잭션 로직(Ledger 생성)이 우회됨. (표준 상점 구매 로직은 정상 작동 검증 완료 `audit_vault_purchases.py`)
*   **해결**:
    1.  **전수 검사**: 상점 내 모든 금고 구매 상품에 대한 자동화 테스트(`audit_vault_purchases.py`) 수행 → 시스템 무결성 검증 완료.
    2.  **데이터 보정**: 누락된 내역을 복구하는 수선 스크립트(`repair_vault_ledger.py`) 개발 및 배포.
    3.  **조치**: 해당 유저(`9660ff34`)에 대해 수선 스크립트 실행 가이드 전달.


---

## 2. ✨ 기능 추가 (New Features)

### A. 미참여 방문자 (No-Play Visitors) 추적 기능
*   **목적**: 사이트에 방문했으나 게임을 돌리지 않은 "잠재 이탈/관망 유저"를 식별하여 리텐션 타겟으로 활용.
*   **구현 로직**:
    1.  **방문자(Visitor) 정의**:
        *   `user_activity.last_login_at` (최근 로그인)
        *   `user_event_log` (버튼 클릭 등 모든 이벤트)
        *   `user_game_wallet_ledger` (재화 변동)
        *   👉 위 3가지 테이블에서 해당 날짜에 기록이 있는 모든 `user_id`를 합집합(Union) 처리.
    2.  **플레이어(Player) 정의**:
        *   `user_game_wallet_ledger`에서 `DICE_PLAY`, `ROULETTE_PLAY`, `LOTTERY_PLAY` 이유로 "차감(delta < 0)"된 유저.
    3.  **미참여 방문자**: `Visitor` - `Player` (차집합).
*   **적용**: 2026-01-18 오전 운영일지부터 "5. 📉 미참여 방문자" 섹션 자동 포함.

---

## 3. 📝 운영/기타
*   **운영서버 동기화 프로세스 강화**: 템플릿 최상단에 `Step 0: 운영서버 데이터 동기화` 절차를 "Warning" 수준으로 강조.
*   **개발/운영 분리**: `2026_core_economy_glossary_ko.md` 등을 참조하여 데이터 조회 로직의 정합성을 지속 검증 예정.

---

## 🔜 Next Steps
*   **[자동화]**: 미참여 방문자 리스트(No-Play)를 기반으로 하는 "자동 넛지(Nudge) DM 발송" 스크립트 검토.
*   **[모니터링]**: 다이아몬드 외 타 인벤토리 아이템(기프티콘 등)도 집계 대상에 포함 검토.
