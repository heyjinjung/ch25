# 도파민05 백엔드 구현 작업 진행 체크리스트

작성일: 2026-01-14  
목적: 도파민05(이벤트 장치) 구현을 “누락 없이, SoT 위반 없이, 운영 리스크 최소화”로 진행하기 위한 백엔드 체크리스트.

---

## 0) 고정 전제 (절대 변경 금지)

- **Vault SoT**: `user.vault_locked_balance`
- **Golden Hour 배율 적용 범위**: **Vault 적립 금액에만 적용** (XP 제외)
- **Ticket SoT**: `user_game_wallet` (GameTokenType별 balance)

---

## 1) 현재 구현 베이스라인 점검 (진행 전 10분)

- [x] `EventService` 존재 여부 및 Golden Hour 판단 로직 확인 (`app/services/event_service.py`)
- [x] Dice 보상 로직 배수 적용 위치 확인 (`app/services/dice_service.py`)
- [ ] Roulette 보상 배수 적용 여부 확인 (`app/services/roulette_service.py`)
- [x] Admin Ops Plan(`ops_*`) 모델/엔드포인트 존재 여부 확인

---

## 2) 도파민05 범위 분해 (구현 단위)

### 2.1 Event Config/Log

- [ ] `event_config` 테이블 생성 및 기본 시드 등록
- [ ] `event_participation_log` 테이블 생성
- [ ] 인덱스 적용(`event_type`, `is_active`, `user_id`, `participated_at`)

### 2.2 Golden Hour 로직

- [x] `EventService.is_golden_hour()` 구현/검증 (server time 기준)
- [x] 세그먼트별 배수 맵 정의 (`VIP/WHALE/COMMON/AT_RISK`) (Dice 기준 적용)
- [ ] Dice/Roulette 승리 보상 배수 적용 (Roulette 미적용)
- [x] Admin FORCE_ON/OFF 반영 (Vault2 config 기반)

### 2.3 Crisis Intervention (Scenario)

- [ ] Scenario 1/3/5 대상자 쿼리 정의 (현재 1/4/11만 카운트/리스트 일부 구현)
- [ ] `CrisisDetectionService` 배치 스케줄 등록
- [x] `ops_target_list` 모델/CRUD/엔드포인트 존재 (`app/models/ops_target.py`, `admin_ops_target.py`)
- [ ] Admin 알림/로그 트리거 설정

### 2.4 Segment Campaign

- [ ] COMMON 전환 트리거 조건 계산(금고 5,000P/0원)
- [ ] VIP 이벤트 트리거 조건(골든아워, 월간 미션)
- [ ] event_config와 정책값 연결

---

## 3) 데이터/로그/관측

- [ ] 이벤트 참여/지급 시 `event_participation_log` 적재
- [ ] 위기 감지/대상자 생성 로그 남김 (ops log 또는 admin audit)
- [ ] KPI 집계를 위한 최소 지표 정의 (CCU, 소비율, 전환율)

---

## 4) 설정/토글/안전장치

- [x] Golden Hour 토글(강제 ON/OFF) 및 배수 조정 API
- [ ] 토글/배수 변경 이력 기록 (ops log)
- [ ] 장애 시 Fail-Open 정책 점검 (UX 이탈 방지)

---

## 5) 테스트/검증 체크리스트

- [ ] `GET /api/events/status` 응답 필드 검증
- [ ] Golden Hour 시간대 진입/종료 경계 테스트
- [ ] FORCE_ON/OFF 및 배수 변경 반영 확인
- [ ] Scenario 대상자 리스트 생성 스모크
