[최종 검토일: 2026-01-26]
정책 최신화 필요 여부: 🟡 정합성 검토 필요 (2026-01-26 기준)

## 정합성 검증 상태 (1~3차 기준)
- 🟢 [정합] BE V2 미션 API: `/api/v2/mission/*` (근거: `app/api/routes/__init__.py`의 `prefix="/api/v2"`, `app/v2/api/routes.py`의 `/mission/*`)
- 🟢 [정합] KST 9AM 리셋: `V2MissionService._operational_play_date()` + DAILY `reset_date` 적용 (근거: `app/v2/services/mission_service.py`, OPS: [docs/v2_specs/05_ops/v2_mission_timezone_fix_20260122_ko.md](../../05_ops/v2_mission_timezone_fix_20260122_ko.md))
- 🟡 [정합성 검토 필요] FE 라우팅 프리픽스: Design 문서에는 `/v2/missions` 표기이나, 현재 FE 라우팅은 `/missions` (근거: `src/router/AppRouter.tsx`, `src/v2/router/V2UserRoutes.tsx`)
- 🟡 [정합성 검토 필요] 레거시 라우터 병존: `/api/mission`, `/api/admin-mission` 라우터가 존재 → v2-only 호출 보장/정리 필요 (근거: `app/api/routes/mission.py`, `app/api/routes/admin_mission.py`)

## 정책 정합성/충돌 처리 원칙
- SoT-코드-운영-DB-프론트 매핑에서 부정합/충돌 발견 시 아래와 같이 명시:
  - 🔴 [정책/구현 충돌]: 구체적 내용, 우선 기준, 임시 조치/추후 액션 명시
  - 🟡 [정합성 검토 필요]: 불일치/불명확/운영상 임시 허용 등
- 근거/판단 기준(SoT/코드/운영/DB/프론트 우선 등) 및 작성일/정책 문서/운영 사례 등 명확히 기입
- 즉시 수정 불가 시 "임시 예외", "운영상 임시 허용", "추후 일괄 정비 필요" 등 TODO로 남김
- 자동화/운영 체크리스트에 해당 부정합이 감지되도록 추가
- 실제 장애/오류/운영 리스크 시 "운영자 주의", "긴급 패치 필요", "QA 우선 검증" 등 강조

## 자동화/운영 체크리스트 & TODO
- [ ] SoT-코드-운영-DB-프론트 매핑 1:1 정합성 검증 자동화 필요
- [ ] FK/UNIQUE/ENUM/미션 정책 등 제약조건 자동 점검
- [ ] 미션/스트릭/보상 실시간 점검 및 운영자 알림 연동
- [ ] 정책/구현/운영 불일치 발견 시 즉시 표기 및 TODO/임시 예외 명시
- [ ] 최신 정책/운영 사례 반영 주기적 검토(작성일/최종 검토일 갱신)
- [ ] 00:00~09:00 KST 경계 회귀 테스트(운영일/LOGIN/클레임) 추가 및 자동화
- [ ] FE 라우팅 `/v2/*` 표기 vs 실제(`/...`) 문서/구현 정합성 확정(문서 갱신 또는 FE 라우팅 변경)
#
...existing code...
# Mission/Attendance 영역 SoT-코드-운영-DB-프론트 매핑 표 (관리자 친화형)

## 1. 미션/스트릭 정책/핵심 Enum/상수/제약조건

### [A] 주요 DB 컬럼/제약조건/Enum
| DB 테이블/컬럼                | 제약조건/Enum/설명                                   | 정책/코드/프론트 매핑 필드명         | 비고 |
|-------------------------------|------------------------------------------------------|--------------------------------------|------|
| user.id                       | PK, UNIQUE                                           | user_id                              | FK 참조시 주의, 공통 PK |
| v2_user.cc_id                 | UNIQUE, NOT NULL                                     | cc_id                                | V2 인증 기준, 로그인 PK |
| user.external_id              | UNIQUE, NOT NULL                                     | external_id                          | 레거시 인증 기준 |
| mission_progress.user_id      | FK(user.id)                                          | user_id                              | FK 제약조건, 인증 연동 주의 |
| user_mission_streak.user_id   | FK(user.id)                                          | user_id                              | FK 제약조건 |
| user_mission.mission_id       | PK, UNIQUE                                           | mission_id                           |      |
| user_mission_streak.streak_id | PK, UNIQUE                                           | streak_id                            |      |
| user_mission.mission_type     | ENUM(MissionType)                                    | MissionType, mission_type            | Enum/케이스 주의 |
| user_mission_streak.streak_type| ENUM(StreakType)                                    | StreakType, streak_type              | Enum/케이스 주의 |
| (기타 FK/UNIQUE/ENUM)         | (각 테이블별로 명시)                                 |                                      |      |

### [B] 미션/유저 연동 구조 요약
| 역할/구분         | 실제 DB 컬럼/정책 | 인증/미션 연동 기준 | 비고 |
|-------------------|-------------------|--------------------|------|
| 공통 PK           | user.id           | 모든 FK/PK 기준    | 미션/레거시 공통 |
| V2 인증 PK        | v2_user.cc_id     | V2 로그인/인증     | 미션 연동시 매핑 필요 |
| 레거시 인증 PK    | user.external_id  | 레거시 인증        | 일부 연동/마이그레이션 주의 |
| 미션 진행 FK      | mission_progress.user_id | 미션 진행/기록 | FK(user.id) 기준, 인증 연동 주의 |

### [C] 프론트-백엔드-DB-코드-정책 1:1 매핑 구조
| 정책/문서           | 실제 코드/Enum/상수         | DB 컬럼/제약조건                | 프론트 필드명         | 비고 |
|---------------------|-----------------------------|----------------------------------|----------------------|------|
| v2_mission_glossary_sot_ko.md | MissionType, StreakType, MISSION_TYPE_DAILY | user_mission.mission_type, user_mission_streak.streak_type | mission_type, streak_type | Enum/케이스 일치 필수 |

---

## 2. 미션 정책
| 구분 | SoT 문서/정책/스키마 | SoT 한글 설명/핵심값/상수/필드 | 실제 코드/핵심 파일 | 운영 상태/테스트/DB/엔드포인트 | DB 적용값 | V1 폐기 | V2 이관 | FE 라우팅 | FE 표시값 | 최신화 일자 | 검증 결과 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 미션 정책 | v2_mission_glossary_sot_ko.md | "미션=DB user_mission, 미션상태/보상/클레임 정책, 미션 단독/스트릭 연동, 수동 클레임" | app/v2/services/mission_service.py | /api/v2/mission/*, test_mission_*.py | | | ✅ 이관 | /missions | 미션 | 2026-01-26 | ✅ 완료 | test_v2_mission_service.py 완료; 🟢 9AM reset(`V2MissionService._operational_play_date`); 🟡 Design 문서 FE `/v2/missions` 표기 정합성 확인 필요; 🟡 레거시 `/api/mission` 라우터 병존 |

## 2. 스트릭 정책/DB
| 구분 | SoT 문서/정책/스키마 | SoT 한글 설명/핵심값/상수/필드 | 실제 코드/핵심 파일 | 운영 상태/테스트/DB/엔드포인트 | DB 적용값 | V1 폐기 | V2 이관 | FE 라우팅 | FE 표시값 | 최신화 일자 | 검증 결과 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 스트릭 정책 | v2_streak_policy_sot_ko.md | "DB: user_mission_streak, 스트릭=연속 출석/미션, 보상: 수동 클레임, 필드: claimable_day" | app/v2/services/streak_service.py (문서 표기) / app/v2/services/mission_service.py (`V2MissionService`) | DB: user_mission_streak | | | | | | | | 🔴 `app/v2/services/streak_service.py` 미존재(현 리포 기준); 현재 streak API(`/api/v2/mission/streak/*`)는 `app/v2/api/routes.py`에서 `V2MissionService`를 사용 |

## 3. 미션/스트릭 상수/Enum
| 구분 | SoT 문서/정책/스키마 | SoT 한글 설명/핵심값/상수/필드 | 실제 코드/핵심 파일 | 운영 상태/테스트/DB/엔드포인트 | DB 적용값 | V1 폐기 | V2 이관 | FE 라우팅 | FE 표시값 | 최신화 일자 | 검증 결과 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 미션/스트릭 상수/Enum | v2_mission_glossary_sot_ko.md | "상수: MISSION_TYPE_DAILY, Enum: MissionType, StreakType, 필드: mission_id, streak_count, claimable_day" | app/v2/services/mission_service.py | DB: user_mission, user_mission_streak | | | | | | | | |

---

## 4. 스트릭 보상 규칙 어드민 API (2026-01-31 추가)

### 백엔드 API
| 엔드포인트 | 메서드 | 설명 | 핵심 파일 |
|------------|--------|------|-----------|
| `/api/v2/admin/ui-config/{key}` | GET | 어드민 UI 설정 조회 | `app/v2/api/admin/ui_config_routes.py` |
| `/api/v2/admin/ui-config/{key}` | PUT | 어드민 UI 설정 갱신 | `app/v2/api/admin/ui_config_routes.py` |

### 주요 설정 키
- `streak_reward_rules`: 스트릭 마일스톤 보상 규칙 JSON
  ```json
  {
    "rules": [
      {
        "day": 3,
        "enabled": true,
        "grants": [{"kind": "WALLET", "token_type": "DICE_TOKEN", "amount": 100}]
      }
    ]
  }
  ```

### 프론트엔드 연동
- 훅: `useAdminUiConfig("streak_reward_rules")`, `useAdminUpdateUiConfig()`
- API 클라이언트: `src/v2/api/adminApi.ts` - `getAdminUiConfig()`, `updateAdminUiConfig()`
- 어드민 페이지: `src/v2/admin/pages/game/MissionManagerPage.tsx` - 스트릭 보상 규칙 설정 카드

### V2 SoT 준수
- ✅ V2 전용 경로 `/api/v2/admin/ui-config/{key}` 사용
- ✅ 레거시 `/admin/api/ui-config/{key}` 와 분리
- ✅ `UiConfigService` 재사용 (app_ui_config 테이블)

---

## 99. 정합성 검증 리포트 (요약)

### 1차 (도메인 매핑표 기반)
- 🟡 FE 라우팅 표기 정합성: 문서(Design) `/v2/missions` ↔ 구현(현재 FE) `/missions` (라우팅 기준 1개로 확정 필요)
- 🟡 레거시 식별자 혼재 리스크(`user.id` vs `v2_user.cc_id` vs `user.external_id`)는 지속 감시 필요

### 2차 (API 계약/공통 SoT 기준)
- 🟢 `/api/v2/mission/*` 계약은 BE 라우팅 구조와 일치
- 🟡 OpenAPI(v2_openapi.yaml)와 실 라우팅의 경로/스키마 자동 비교는 TODO

### 3차 (OPS/Design 기반)
- 🟢 KST 9AM 리셋 로직이 `V2MissionService`에 구현되어 split-brain 방지 방향과 일치
- 🟡 00:00~09:00 KST 경계(로그인/클레임/진행도) 회귀 테스트 자동화가 필요

---

## 100. 실전 코드베이스 체크 리포트 (app/, src/)

### ✅ 적용/구현 확인(정합)
- 🟢 FE(신규): `src/v2/api/missionApi.ts`, `src/v2/hooks/useV2Mission.ts`가 `/api/v2/mission/*` 사용
- 🟢 FE(신규): `src/v2/pages/missions/MissionsPage.tsx`가 `useV2Missions()` 기반으로 실제 데이터 렌더 + Claim 후 `refetch()` 수행
- 🟢 BE(V2): `app/api/routes/__init__.py`에서 `v2_router`가 `/api/v2` prefix로 등록되어 `/api/v2/mission/*` 경로가 유효
- 🟢 BE(V2): `app/v2/api/routes.py`에 `/mission/`, `/mission/{mission_id}/claim`, `/mission/streak/*` 구현 + `V2MissionService` 사용
- 🟢 Admin(V2): `app/v2/api/admin/__init__.py` prefix=`/admin` + `app/v2/api/admin/mission_routes.py`의 `/game/missions` → `/api/v2/admin/game/missions`로 동작

### ⚠️ 불일치/레거시/임시 예외(문서에 반영 필요)
- 🟡 FE 라우팅: Design 문서 `/v2/missions` 표기 vs 실제 앱 라우팅은 `/missions` (root에 V2UserRoutes mount)
- 🟡 FE 레거시 호출 잔존: `src/stores/missionStore.ts`, `src/api/streakApi.ts`가 `/api/mission/*` 사용(현재 v2 미션 화면은 미사용이지만 코드베이스에 잔존)
- 🟡 BE 레거시 라우터 병존: `app/api/routes/mission.py` prefix=`/api/mission` 존재(향후 v2-only 보장 관점에서 정리/차단 검토)
- 🟡 BE alias/stub 엔드포인트 존재: `app/v2/api/routes.py`에 `/mission/list`, `/mission/claim` 등이 존재하며 특히 `/mission/claim`은 현재 stub 반환(문서/계약에 없는 경로)

### 🧪 자동화 필요(우선순위)
- 🔥 00:00~09:00 KST 경계 회귀(운영일/리셋/클레임 가능 상태) 자동화
- 🧹 `/api/mission/*`(레거시) 호출이 실제 유저 플로우에 섞이지 않도록 grep 기반 감시(빌드/CI) 또는 삭제/차단 정책 확정
