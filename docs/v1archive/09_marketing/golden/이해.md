# 골든 프로젝트 쉬운 설명서

어제 이벤트 시작
오늘 접속 수 급감
운영 로그 확인 → 이벤트가 원인일 가능성 큼
롤백 실행
다음 날 수치 회복 확인
리포트에 “이벤트 중지로 회복됨” 기록

) 시간 흐름으로 보는 전체 흐름
상황 발생

유저 행동이 변한다. (접속 감소, 참여 감소 등)
모니터링 기준 확인

정해둔 숫자를 계속 본다.
“정상인지 이상인지”를 확인한다.
경고 기준 도달

숫자가 위험한 수준이면 경고가 뜬다.
“지금 뭔가 이상하다”를 알게 된다.
원인 추정

운영 로그를 본다.
“어제 누가 어떤 이벤트를 켰는지” 확인한다.
변화가 시작된 시점을 찾는다.
대응 선택

문제 크기가 작으면 조정
크면 롤백(되돌리기)
실행 리포트 기록

“언제, 무엇을 했고, 결과가 어땠는지” 문서로 남긴다.

## 1. 골든 프로젝트가 뭐야?
골든 프로젝트는 **사람들이 서비스를 더 자주, 더 오래 쓰게 만들기** 위한 운영 시스템이야.  
쉽게 말하면,  
**“유저를 다시 돌아오게 만들고, 그 결과를 기록하고, 효과가 있었는지 확인하는 시스템”**이야.

---

## 2. 골든 프로젝트가 하는 일 3가지

### ① 개입(Intervention)
**의미:** 유저 상태를 보고, 적절한 타이밍에 “도와주는 행동”을 하는 것

- 유저가 **게임을 오래 안 하면**
  → **알림/메시지**를 보내서 다시 오게 함  
- 유저가 **지칠 타이밍이면**
  → **보상**을 줘서 다시 참여하게 함  
- 유저가 **특정 행동을 하면**
  → **이벤트**를 실행해서 재미를 더함  

즉, **유저 행동을 보고 “맞춤형 반응”을 해주는 체계**야.

---

### ② 관제(Operations)
**의미:** 운영자가 한 일을 모두 기록하고, 문제가 생기면 빠르게 찾는 것

- 운영자가 어떤 버튼을 눌렀는지  
- 언제 어떤 이벤트를 실행했는지  
- 어떤 메시지를 누구에게 보냈는지  

이걸 **운영 로그**로 저장해.  
그래서 문제가 생기면 **“누가, 언제, 무엇을 했는지”** 바로 찾을 수 있어.

관제에는 이런 도구가 포함돼:
- **관리자 화면**
- **CSV 업로드 기능**
- **대시보드**

즉, **운영이 투명하고 빠르게 확인되도록 만드는 체계**야.

---

### ③ 증거 수집(Evidence)
**의미:** “진짜 효과가 있었는지” 데이터를 모아서 증명하는 것

예를 들어:
- 메시지를 보냈더니 **유저가 돌아왔는가?**
- 보상을 줬더니 **게임 횟수가 늘었는가?**
- 이벤트가 효과가 있었는가?

이걸 확인하려면 **데이터와 기준**이 필요해.

그래서 골든 프로젝트는  
- **모니터링 기준**: 평소에 어떤 숫자를 계속 보는지 정한 규칙이야.  
  로그나 대시보드에는 보통 **시간, 분류, 대상, 수치** 같은 필드가 있고,  
  그중 **접속수/참여율/오류율** 같은 숫자를 꾸준히 확인해.  
  예를 들면 “하루 접속 인원이 갑자기 줄었는가?”처럼  
  **정상인지 이상인지**를 계속 확인하는 기준이야.
- **경고 기준**: 문제가 “위험 수준”에 왔는지 알려주는 기준이야.  
  여기엔 미리 정해둔 **임계값(상수)**가 있어.  
  예를 들어 “참여율이 임계값 아래로 내려가면 경고”처럼 작동해.  
  예시로는 “일일 접속수가 평소보다 30% 이상 감소”,  
  “오류율이 5% 이상”, “이벤트 참여율이 기준치 이하” 같은 식이야.  
  그래서 운영자는 **바로 알림을 받고 대응**할 수 있어.
- **롤백 기준**: 너무 문제가 크면 **되돌리기(취소)** 하는 기준이야.  
  예를 들어 “이벤트를 시작했더니 지표가 급격히 나빠지면 즉시 끈다” 같은 규칙이야.  
  즉, **큰 사고를 막기 위한 안전장치**야.
- **실행 리포트 문서**: 실제로 무엇을 했고, 결과가 어땠는지 적는 보고서야.  
  “언제, 어떤 행동을 했고, 유저가 어떻게 반응했는지”를 기록해서  
  **다음에 더 잘 할 수 있게** 도와줘.

같은 걸 **전부 연결해서 관리**해.

즉,  
**“좋은 결과가 실제로 나왔다는 증거를 쌓는 체계”**야.

---

## 3. 한 줄 요약
**골든 프로젝트는 “유저를 다시 오게 만들기 위한 실행 + 기록 + 검증”을 하나로 묶은 운영 시스템이다.**

---

## 4. 왜 중요한가?
이 시스템이 있으면:

✅ 운영이 **감으로만** 이루어지지 않음  
✅ 어떤 일이 있었는지 **기록이 남음**  
✅ 효과가 있었는지 **숫자로 확인 가능**  
✅ 문제가 생기면 **즉시 대응 가능**  

---

## 5. 아주 쉬운 예시로 설명하면
**상황:**  
유저가 요즘 게임을 안 들어온다.

**골든 프로젝트가 하는 일:**  
1) **개입:** “돌아오면 보상 줄게!” 메시지를 보냄  
2) **관제:** 누가 언제 보냈는지 기록  
3) **증거 수집:** 메시지를 보고 유저가 다시 왔는지 확인  

이걸 반복하면,  
“어떤 행동이 진짜 효과 있는지”도 알 수 있어.

---

## 6. 결론
골든 프로젝트는 단순한 기능 하나가 아니라,  
**운영 전체를 똑똑하게 만드는 시스템**이야.

**유저를 돌아오게 만들고,  
기록을 남기고,  
효과를 증명하는 구조**가 핵심이야.




+
📋 HQ Margin → Analytics Dashboard 실시간 연동 구현 계획서
문서 타입: Implementation Plan / Technical Spec
도메인: Golden / Ops / Analytics
작성일: 2026-02-02
상태: PLAN → PATCH

1. 현재 상태 분석 (Current Status)
1.1 기존 구현
컴포넌트	파일	상태	문제점
HQ Margin CSV Import	hq_margin_import_service.py	✅ 완료	V2UserSegment에 저장됨
V2UserSegment 모델	v2_user_segment.py	✅ 완료	total_margin, total_charge 컬럼 추가됨
GameLogAnalyticsService	game_log_analytics_service.py	✅ 완료	V2GameLog 기반 (빈 테이블)
분석 대시보드	AnalyticsDashboard.tsx	✅ 완료	오늘 수익 = ₩0 (데이터 미연동)
Ops Dashboard	OpsDashboard.tsx	✅ 완료	기회 그룹 상세 미표시
1.2 문제 정의
데이터 소스 불일치: Dashboard는 V2GameLog를 쿼리 → 테이블 비어있음 → ₩0
HQ Margin 미활용: V2UserSegment에 total_margin, total_charge 있음 → 집계 안됨
Risk/Opportunity 미연동: inactive_days 기반 위험 유저, VIP/WHALE 기회 유저 리스트 미노출
2. 핵심 데이터 SoT
2.1 HQ Margin CSV 컬럼 → V2UserSegment 매핑
CSV 컬럼	DB 컬럼	의미
총 운영 마진	total_margin	충전 - 환전 (핵심 수익!)
누적 충전 금액	total_charge	총 충전액
미접속 경과일	inactive_days	이탈 위험 판단 기준
2.2 세그먼트 분류 SoT
세그먼트	조건	골든 전략
VIP	마진 100만원+	고배율 골든아워, 우선 지원
WHALE	충전 500만원+	VIP 전환 유도
AT_RISK	7일+ 미접속 + 마진 양수	자동 리워드, 복귀 캠페인
COMMON	기본	일반 운영
3. 구현 상세 (Implementation Details)
3.1 Task 1: HQ Margin Summary API (P0, 30분)
파일: game_log_analytics_service.py
def get_hq_margin_summary(self) -> dict:
    """
    HQ Margin CSV 기반 전체 수익 통계
    
    데이터 소스: V2UserSegment (total_margin, total_charge)
    
    Returns:
        {
            "total_margin": int,         # 전체 운영 마진
            "total_charge": int,         # 전체 충전액  
            "total_withdrawal": int,     # 전체 환전액 (charge - margin)
            "vip_count": int,            # VIP 유저 수
            "whale_count": int,          # WHALE 유저 수
            "at_risk_count": int,        # AT_RISK 유저 수
            "active_users": int,         # 7일 내 활성 유저
            "synced_users": int,         # HQ 동기화 유저 수
            "last_synced_at": datetime,  # 마지막 동기화 시각
        }
    """



3.2 Task 2: Revenue Stats 통합 (P0, 30분)
파일: ops_routes.py
# 기존 OpsStatusResponse에 revenueStats 추가
class RevenueStatsDto(BaseModel):
    # HQ Margin 기반 (실데이터)
    total_margin: int            # 총 운영 마진 (CSV)
    total_charge: int            # 총 충전액 (CSV)
    total_withdrawal: int        # 총 환전액 (계산)
    
    # Game Log 기반 (보조)
    today_bet: int               # 오늘 배팅액 (있으면)
    today_payout: int            # 오늘 지급액 (있으면)
    
    # 통합 지표
    today_revenue: int           # 오늘 수익 = total_margin (HQ 기준)
    today_expenses: int          # 오늘 지출 = total_withdrawal
    weekly_growth_rate: float    # 주간 성장률
3.3 Task 3: 주간 성장률 계산 (P1, 20분)
로직: HQ Margin 스냅샷 비교 (이번주 vs 전주)
def get_weekly_growth_rate(self) -> float:
    """
    주간 성장률 = (이번주 total_margin - 전주 total_margin) / 전주 * 100
    
    Note: HQ Margin CSV가 정기적으로 업로드되므로,
          last_synced_at 기준 7일 전 스냅샷과 비교
    """
3.4 Task 4: 리텐션 분석 연동 (P1, 20분)
파일: game_log_analytics_service.py
def get_risk_users(self, limit: int = 10) -> List[dict]:
    """
    이탈 위험 유저 상세 리스트
    
    조건: inactive_days > 7 AND total_margin > 0
    
    Returns:
        [
            {
                "user_id": int,
                "nickname": str,
                "segment": str,
                "inactive_days": int,
                "total_margin": int,
                "risk_level": "HIGH" | "MEDIUM",
            }
        ]
    """

def get_opportunity_users(self, limit: int = 10) -> List[dict]:
    """
    기회 유저 상세 리스트 (VIP/WHALE)
    
    Returns:
        [
            {
                "user_id": int,
                "nickname": str,
                "segment": str,  # VIP | WHALE
                "total_margin": int,
                "total_charge": int,
            }
        ]
    """
3.5 Task 5: ROI 서비스 연동 (P2, 20분)
파일: 기존 roi_analysis_service.py 활용
def get_risk_users(self, limit: int = 10) -> List[dict]:
    """
    이탈 위험 유저 상세 리스트
    
    조건: inactive_days > 7 AND total_margin > 0
    
    Returns:
        [
            {
                "user_id": int,
                "nickname": str,
                "segment": str,
                "inactive_days": int,
                "total_margin": int,
                "risk_level": "HIGH" | "MEDIUM",
            }
        ]
    """

def get_opportunity_users(self, limit: int = 10) -> List[dict]:
    """
    기회 유저 상세 리스트 (VIP/WHALE)
    
    Returns:
        [
            {
                "user_id": int,
                "nickname": str,
                "segment": str,  # VIP | WHALE
                "total_margin": int,
                "total_charge": int,
            }
        ]
    """
4. 전체 데이터 흐름
5. Allowed Files
┌──────────────────┐
│ HQ Margin CSV    │   Result (1).CSV (실제 마진 데이터)
│ 199 유저         │
└────────┬─────────┘
         │ Admin Upload / import
         ▼
┌──────────────────────────────────────────┐
│ V2UserSegment (DB)                       │
│ • total_margin    (총 마진)              │
│ • total_charge    (총 충전)              │
│ • inactive_days   (미접속일)             │
│ • segment         (VIP/WHALE/AT_RISK)    │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ GameLogAnalyticsService (신규 메서드)     │
│ • get_hq_margin_summary()               │
│ • get_risk_users()                       │
│ • get_opportunity_users()               │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ ops_routes.py - /api/v2/admin/ops/status │
│ • revenueStats: RevenueStatsDto          │
│ • riskUsers: List[RiskUserDto]           │
│ • opportunityUsers: List[OpportunityUserDto]│
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ AnalyticsDashboard.tsx                   │
│ • 오늘 수익: ₩XX,XXX,XXX (실데이터!)    │
│ • 오늘 지출: ₩XX,XXX,XXX               │
│ • 주간 성장률: +X.X%                    │
│ • 위험 유저: N명                         │
│ • 기회 그룹: N명                         │
└──────────────────────────────────────────┘
백엔드
파일	작업
game_log_analytics_service.py	신규 메서드 추가
ops_routes.py	revenueStats, riskUsers 연동
app/v2/schemas/ops_schema.py	DTO 추가/수정
프론트엔드
파일	작업
src/admin/pages/dashboard/AnalyticsDashboard.tsx	HQ Margin 데이터 표시
src/admin/types/ops.ts	타입 추가
1. Out of Scope
HQ Margin CSV Import 로직 수정 (이미 완료)
V2UserSegment 모델 변경 (이미 완료)
신규 마이그레이션 (불필요)
Game Log CSV 실시간 연동 (별도 태스크)
1. 예상 시간
Task	우선순위	예상 시간
Task 1: HQ Margin Summary API	P0	30분
Task 2: Revenue Stats 통합	P0	30분
Task 3: 주간 성장률 계산	P1	20분
Task 4: 리텐션 분석 연동	P1	20분
Task 5: ROI 연동	P2	20분
검증 및 배포	-	30분
총계		~2시간 30분
1. 변경 이력
v1.0 (2026-02-02, GitHub Copilot): 최초 작성 - HQ Margin Dashboard 연동 계획서


✨ 지민코드 V2 업데이트 완료! 혜택 맛집 오픈! 풍성한 보너스와 특별 미션이 여러분을 기다립니다. 지금 참여하고 모든 혜택을 싹쓸이 하세요! 🎁"
강력한 서비스 변화 강조형 "🔥 드디어 공개된 V2 인터페이스! 더 강력해진 혜택과 독보적인 서비스로 무장했습니다. 망설이지 마시고 지금 바로 역대급 이벤트의 주인공이 되어보세요! 🚀"
 CC25 V2 그랜드 오픈! 기다려주신 만큼 역대급으로 쏩니다! 지금 바로 쏟아지는 보너스 혜택과 프리미엄 서비스를 마음껏 경험해 보세요! 💎"